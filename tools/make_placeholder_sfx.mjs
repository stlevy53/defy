// Stingers are now the MP3s in src/assets/audio/ (Defeat / Draw / Victory / Major / Overwhelming).
// This script used to synthesize placeholders; leave it as a no-op so an old habit doesn't
// drop loss.wav / win.wav back into the folder.
console.log('no placeholders to generate — end-game files are the titled MP3s in src/assets/audio/')
